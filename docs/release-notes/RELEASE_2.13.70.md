# Samara ERP 2.13.70

Display Breakfast, Lunch and Dinner cutoff notices as separate full-width rows on all screen sizes. Each row includes its schedule and an explicitly bold Open or Closed status. The order-form cutoff notice now also includes the bold status. Cutoff rules and permissions are unchanged.

Validation: JavaScript syntax and browser rendering of the actual Food Vendor component with mock data.

Publish food-vendor.js, app.js, index.html and service-worker.js. No backend changes.
