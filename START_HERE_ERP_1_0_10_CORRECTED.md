# Samara Care ERP 1.0.10 — Corrected Version Label

This package corrects stale 1.0.8 labels that remained inside app.js, index.html and manifest.webmanifest.

After deployment, the login page and sidebar must display **1.0.10**.
The service-worker cache name is **samara-erp-1.0.10-corrected** to force a fresh load.

Upload the contents of this folder to the repository root, replacing the existing files.
